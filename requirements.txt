# LLM clients used by every language-model agent (OpenAI Responses API / Google Gemini).
openai>=1.66.0
google-genai>=1.0.0
python-dotenv>=1.0.0

# src/dataset/* (benchmark dataset construction pipeline, see src/dataset/build_dataset.py).
pydantic>=2.0
httpx>=0.27
tenacity>=8.0
PyYAML>=6.0

# Dev/test only, used by test/dataset/*.
pytest>=8.0
